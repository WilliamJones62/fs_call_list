<%= form_with(model: on_special, local: true) do |form| %>
  <% if on_special.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(on_special.errors.count, "error") %> prohibited this on_special from being saved:</h2>

      <ul>
      <% on_special.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <thead>
      <tr>
        <th> <%= form.label :customer, "Customer code" %> </th>
        <th> <%= form.label :part, "Part code" %> </th>
        <th> <%= form.label :onspecials_start, "Start date" %> </th>
        <th> <%= form.label :onspecials_end, "End date" %> </th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td> <%= form.select :customer, @customer, {}, {:value => params[:customer], :id => "on_special_customer", :onChange=> ('onSpecialLists()')} %> </td>
        <td> <%= form.select :part, @part, {}, {:value => params[:part], :id => "on_special_part"} %> </td>
        <td> <%= form.date_select :onspecials_start, id: :on_special_onspecials_start %> </td>
        <td> <%= form.date_select :onspecials_end, id: :on_special_onspecials_end %> </td>
      </tr>
    </tbody>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<p id= "customers" hidden> <%= $allcust %> </p>
<p id= "customersA" hidden> <%= $allcustA %> </p>
<p id= "parts" hidden> <%= $allpart %> </p>
