<%= form_with(model: isr_list, local: true) do |form| %>
  <% if isr_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(isr_list.errors.count, "error") %> prohibited this isr_list from being saved:</h2>

      <ul>
      <% isr_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :isr_list_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
